---
import { BOOKING_URL } from '../config/site';
---

<section class="contact-page-centered" id="contact-top">
  
  <!-- En-tête de page : Titre & sous-titre à gauche, bouton à droite -->
  <div class="w-layout-blockcontainer container w-container">
    <div class="contact-page-header" data-coaching-reveal>
      
      <div class="contact-page-header__left">
        <div class="approche-badge contact-badge">
          <span class="approche-badge-dot"></span>
          Contact &amp; Cabinet
        </div>
        
        <h1 class="contact-page-title">Prendre contact avec Anne Chataigner</h1>
        
        <p class="contact-page-subtitle">
          Une première rencontre permet de faire le point sur votre situation et de voir si l’accompagnement proposé vous correspond.
        </p>
      </div>

      <div class="contact-page-header__right">
        <a href={BOOKING_URL} class="primary-button contact-btn--primary w-inline-block" data-booking-link>
          <div class="button-text-wrapper">
            <div class="button-text-inner">
              <div class="button-text">Prendre rendez-vous en ligne</div>
              <div class="button-text">Prendre rendez-vous en ligne</div>
            </div>
          </div>
        </a>
      </div>

    </div>
  </div>

  <!-- Section Pleine Largeur avec Image de Fond & Formulaire Compact Centré -->
  <div class="contact-fullwidth-backdrop" id="formulaire">
    <div class="contact-backdrop-overlay"></div>

    <div class="w-layout-blockcontainer container w-container contact-backdrop-container">
      
      <!-- Carte Formulaire Compacte -->
      <div class="contact-form-modal-card" data-coaching-reveal>
        <header class="contact-form-modal-card__header">
          <h2 class="contact-form-modal-card__title">Envoyer un message</h2>
          <p class="contact-form-modal-card__lead">
            Posez votre question ou détaillez votre démarche en toute confidentialité.
          </p>
        </header>

        <form 
          class="contact-form" 
          id="ac-contact-form" 
          method="POST" 
          action="/api/contact"
          novalidate
        >
          <!-- Ligne 1 : Nom & Email -->
          <div class="contact-form__row contact-form__row--2cols">
            <div class="contact-form__group">
              <label for="contact-name" class="contact-form__label">
                Nom &amp; prénom <span class="contact-form__required" aria-hidden="true">*</span>
              </label>
              <input 
                type="text" 
                id="contact-name" 
                name="name" 
                class="contact-form__input" 
                placeholder="Ex. Marie Dupont" 
                required 
                autocomplete="name"
                aria-required="true"
              />
              <span class="contact-form__error" id="error-name" aria-live="polite"></span>
            </div>

            <div class="contact-form__group">
              <label for="contact-email" class="contact-form__label">
                Adresse e-mail <span class="contact-form__required" aria-hidden="true">*</span>
              </label>
              <input 
                type="email" 
                id="contact-email" 
                name="email" 
                class="contact-form__input" 
                placeholder="votre.email@exemple.fr" 
                required 
                autocomplete="email"
                aria-required="true"
              />
              <span class="contact-form__error" id="error-email" aria-live="polite"></span>
            </div>
          </div>

          <!-- Ligne 2 : Téléphone, Type d'accompagnement & Format (3 colonnes sur desktop) -->
          <div class="contact-form__row contact-form__row--3cols">
            <div class="contact-form__group">
              <label for="contact-phone" class="contact-form__label">
                Téléphone <span class="contact-form__optional">(facultatif)</span>
              </label>
              <input 
                type="tel" 
                id="contact-phone" 
                name="phone" 
                class="contact-form__input" 
                placeholder="06 00 00 00 00" 
                autocomplete="tel"
              />
            </div>

            <div class="contact-form__group">
              <label for="contact-service" class="contact-form__label">
                Accompagnement <span class="contact-form__optional">(facultatif)</span>
              </label>
              <div class="contact-form__select-wrap">
                <select id="contact-service" name="service" class="contact-form__select">
                  <option value="">Sélectionnez...</option>
                  <option value="coaching">Coaching individuel</option>
                  <option value="therapies">Thérapies brèves</option>
                  <option value="hypnose">Hypnose Ericksonienne</option>
                  <option value="couple">Consultation de couple</option>
                  <option value="hesitation">Je ne sais pas encore</option>
                </select>
                <span class="contact-form__select-arrow" aria-hidden="true">▾</span>
              </div>
            </div>

            <div class="contact-form__group">
              <label for="contact-preference" class="contact-form__label">
                Format <span class="contact-form__optional">(facultatif)</span>
              </label>
              <div class="contact-form__select-wrap">
                <select id="contact-preference" name="preference" class="contact-form__select">
                  <option value="cabinet">Au cabinet</option>
                  <option value="visio">En visioconférence</option>
                  <option value="indifferent">Indifférent</option>
                </select>
                <span class="contact-form__select-arrow" aria-hidden="true">▾</span>
              </div>
            </div>
          </div>

          <!-- Ligne 3 : Message -->
          <div class="contact-form__group">
            <label for="contact-message" class="contact-form__label">
              Votre message <span class="contact-form__required" aria-hidden="true">*</span>
            </label>
            <textarea 
              id="contact-message" 
              name="message" 
              class="contact-form__textarea" 
              rows="3" 
              placeholder="Expliquez brièvement votre démarche ou posez votre question..." 
              required 
              aria-required="true"
              aria-describedby="help-message"
            ></textarea>
            <p class="contact-form__help-text" id="help-message">
              Évitez de transmettre des informations médicales sensibles dans ce formulaire.
            </p>
            <span class="contact-form__error" id="error-message" aria-live="polite"></span>
          </div>

          <!-- Ligne 4 : Consentement & Bouton Envoyer -->
          <div class="contact-form__bottom-row">
            <div class="contact-form__consent-group">
              <label class="contact-form__consent-label">
                <input 
                  type="checkbox" 
                  id="contact-consent" 
                  name="consent" 
                  class="contact-form__checkbox" 
                  required 
                  aria-required="true"
                />
                <span class="contact-form__consent-text">
                  J'accepte le traitement de mes coordonnées pour répondre à ma demande (<a href="/politique-confidentialite" target="_blank" rel="noopener">confidentialité</a>).
                </span>
              </label>
              <span class="contact-form__error" id="error-consent" aria-live="polite"></span>
            </div>

            <div class="contact-form__action">
              <button type="submit" class="primary-button contact-btn--submit" id="submit-btn">
                <span class="button-text-wrapper">
                  <span class="button-text-inner">
                    <span class="button-text">Envoyer ma demande</span>
                    <span class="button-text">Envoyer ma demande</span>
                  </span>
                </span>
              </button>
            </div>
          </div>

          <!-- Feedback de statut -->
          <div class="contact-form__status" id="form-status" role="status" aria-live="polite"></div>

        </form>

        <!-- Repères essentiels en bas de carte -->
        <footer class="contact-form-modal-card__footer">
          <div class="contact-form-footer-item">
            <svg width="14" height="14" viewBox="0 0 20 20" fill="none" aria-hidden="true"><path d="M16.5 8.2c0 4.1-6.5 8.8-6.5 8.8S3.5 12.3 3.5 8.2a6.5 6.5 0 1 1 13 0Z" stroke="currentColor" stroke-width="1.3"></path><circle cx="10" cy="8" r="2.1" stroke="currentColor" stroke-width="1.3"></circle></svg>
            <span>2 Rue de la Bergerie, 78960 Voisins-le-Bretonneux</span>
          </div>
          <span class="contact-form-footer-dot" aria-hidden="true">·</span>
          <div class="contact-form-footer-item">
            <svg width="14" height="14" viewBox="0 0 20 20" fill="none" aria-hidden="true"><polygon points="17 6 12 10 17 14 17 6" stroke="currentColor" stroke-width="1.3"></polygon><rect x="3" y="5" width="10" height="10" rx="2" stroke="currentColor" stroke-width="1.3"></rect></svg>
            <span>Visioconférence sécurisée</span>
          </div>
        </footer>

      </div>

    </div>
  </div>

</section>

<script is:inline>
  (() => {
    const form = document.getElementById('ac-contact-form');
    const statusEl = document.getElementById('form-status');
    const submitBtn = document.getElementById('submit-btn');
    if (!form || !statusEl) return;

    form.addEventListener('submit', (e) => {
      e.preventDefault();
      
      const nameInput = document.getElementById('contact-name');
      const emailInput = document.getElementById('contact-email');
      const messageInput = document.getElementById('contact-message');
      const consentInput = document.getElementById('contact-consent');
      const errorName = document.getElementById('error-name');
      const errorEmail = document.getElementById('error-email');
      const errorMessage = document.getElementById('error-message');
      const errorConsent = document.getElementById('error-consent');

      if (errorName) errorName.textContent = '';
      if (errorEmail) errorEmail.textContent = '';
      if (errorMessage) errorMessage.textContent = '';
      if (errorConsent) errorConsent.textContent = '';
      statusEl.className = 'contact-form__status';
      statusEl.textContent = '';

      let hasError = false;

      if (!nameInput.value.trim()) {
        if (errorName) errorName.textContent = 'Veuillez renseigner votre nom et prénom.';
        hasError = true;
      }

      const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
      if (!emailInput.value.trim() || !emailRegex.test(emailInput.value.trim())) {
        if (errorEmail) errorEmail.textContent = 'Veuillez renseigner une adresse e-mail valide.';
        hasError = true;
      }

      if (!messageInput.value.trim()) {
        if (errorMessage) errorMessage.textContent = 'Veuillez saisir votre message.';
        hasError = true;
      }

      if (!consentInput.checked) {
        if (errorConsent) errorConsent.textContent = 'Votre consentement est requis.';
        hasError = true;
      }

      if (hasError) {
        statusEl.className = 'contact-form__status contact-form__status--error';
        statusEl.textContent = 'Veuillez corriger les champs signalés avant d\'envoyer.';
        return;
      }

      statusEl.className = 'contact-form__status contact-form__status--pending';
      statusEl.textContent = 'Préparation de votre message...';
      if (submitBtn) submitBtn.setAttribute('disabled', 'true');

      const formData = new FormData(form);
      const service = formData.get('service') || 'Non précisé';
      const preference = formData.get('preference') || 'Non précisé';
      const phone = formData.get('phone') || 'Non renseigné';
      const subject = encodeURIComponent(`Demande de contact — ${nameInput.value.trim()}`);
      const body = encodeURIComponent(
        `Nom: ${nameInput.value.trim()}\n` +
        `E-mail: ${emailInput.value.trim()}\n` +
        `Téléphone: ${phone}\n` +
        `Accompagnement: ${service}\n` +
        `Format souhaité: ${preference}\n\n` +
        `Message:\n${messageInput.value.trim()}`
      );

      setTimeout(() => {
        statusEl.className = 'contact-form__status contact-form__status--success';
        statusEl.innerHTML = 'Votre messagerie va s\'ouvrir pour finaliser l\'envoi.<br><small>Vous pouvez également prendre rendez-vous directement en ligne.</small>';
        window.location.href = `mailto:contact@anne-chataigner.fr?subject=${subject}&body=${body}`;
        if (submitBtn) submitBtn.removeAttribute('disabled');
      }, 500);
    });
  })();
</script>
